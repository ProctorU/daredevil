machine:
  ruby:
    version: 2.5.1
  timezone:
    America/Chicago

database:
  override:
    - 'bundle exec rake --rakefile test/dummy/Rakefile db:setup'

test:
  override:
    - 'bundle exec rake'
